#!/bin/bash

# TODO: remove after check
# export NVM_DIR=~/.nvm
#
# source "$NVM_DIR/nvm.sh"
# nvm use default
#
# source scripts/setup_env
# exec node index.js --config="${ALTEXO_CHAT_CONFIG}"
mkdir -p log
npm install
exec node index.js --config="$1"
